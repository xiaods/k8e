# SonarCloud project configuration
sonar.projectKey=xiaods_k8e

# Exclude generated protobuf code, vendored libraries and lock files (generated/mechanical —
# never counted for duplication or issue analysis)
sonar.exclusions=pkg/sandboxmatrix/grpc/pb/**,pkg/apis/**,pkg/generated/**,pkg/deploy/zz_generated_bindata.go,tandem/vendor/**,**/package-lock.json,**/pnpm-lock.yaml

# dsh plugin tools follow the framework-mandated defineTool shape (each tool
# carries name/description/parameters/output/execute); the structural
# similarity is API boilerplate, not copy-paste duplication.
sonar.cpd.exclusions=plugins/deepseek-harness/packages/dsh-k8e-sandbox-tool/**,tandem/vendor/**,**/package-lock.json,**/pnpm-lock.yaml
