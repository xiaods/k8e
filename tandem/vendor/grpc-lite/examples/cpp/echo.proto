syntax = "proto3";

package demo;

message EchoRequest {
  string message = 1;
}

message EchoReply {
  string message = 1;
}

service EchoService {
  rpc Echo(EchoRequest) returns (EchoReply);
  rpc Missing(EchoRequest) returns (EchoReply);
}
